/**
 * ui/Loaders.jsx
 * Unified redirection to Precision Enterprise shared design system loaders.
 * Eliminates duplicate implementation while maintaining zero breaking changes.
 */
export {
  LoadingOverlay,
  PageLoader,
  SkeletonLoader,
  Spinner,
} from '../shared/Skeleton';
